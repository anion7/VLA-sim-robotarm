# AGENTS.md

## Cursor Cloud specific instructions

This repository (**VLA-sim-robotarm**) is a project for Vision-Language-Action (VLA) models with NVIDIA Isaac Sim for robot arm simulation. As of the initial setup, the repo contains only a `README.md` — no source code, dependencies, tests, or build configuration exist yet.

- **Language/ecosystem**: Expected to be Python-based (VLA/ML typically uses PyTorch). The VM has Python 3.12 available.
- **No dependency files**: There are no `requirements.txt`, `pyproject.toml`, `setup.py`, `package.json`, or similar files. When code is added, the update script should be revised accordingly.
- **No services to run**: There are currently no applications, servers, or services to start.
- **No lint/test/build**: No linting, testing, or build tooling is configured. These should be added as the project develops.
